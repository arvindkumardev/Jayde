import { StyleSheet } from 'react-native';
import {RfW} from '../../utils/helpers';

const styles = StyleSheet.create({
    checkBoxCircle:{backgroundColor: '#ffffff',
        borderStyle: 'solid',
        borderWidth: 2,
        borderColor: '#707070',
        height:RfW(24),
        width:RfW(24),
        borderRadius:RfW(24)}
});

export default styles;
